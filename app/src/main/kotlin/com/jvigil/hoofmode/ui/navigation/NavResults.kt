package com.jvigil.hoofmode.ui.navigation

/**
 * Key used to pass a picked exercise id back through the previous back stack entry's
 * SavedStateHandle — the standard Navigation-Compose "return a result" pattern.
 */
const val SELECTED_EXERCISE_ID = "selected_exercise_id"
